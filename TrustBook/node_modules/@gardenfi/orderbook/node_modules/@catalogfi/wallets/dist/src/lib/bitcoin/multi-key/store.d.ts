export type IWalletStore = {
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<void>;
};
export declare class EncryptedStore implements IWalletStore {
    private static readonly salt;
    private store;
    private key;
    constructor(store: IWalletStore, password: string);
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<void>;
}
export declare class WalletMemoryStore implements IWalletStore {
    #private;
    constructor();
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<void>;
}
