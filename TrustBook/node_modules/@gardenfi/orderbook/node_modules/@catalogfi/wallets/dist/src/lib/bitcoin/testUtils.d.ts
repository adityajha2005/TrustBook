import { Signer, Wallet } from 'ethers';
import * as bitcoin from 'bitcoinjs-lib';
export declare const getTypedData: (nonce: number, network: number) => {
    types: {
        Data: {
            name: string;
            type: string;
        }[];
    };
    domain: {
        name: string;
        version: string;
        chainId: number;
    };
    message: {
        Message: string;
        Version: string;
        Nonce: number;
    };
};
export declare const getSigner: (wallet: Signer | Wallet, nonce: number, network: bitcoin.Network) => Promise<import('ecpair').ECPairInterface>;
export declare const getSignerFromPk: (pk: string, network: bitcoin.Network) => Promise<import('ecpair').ECPairInterface>;
export declare const getScript: (network: bitcoin.Network, type: 'p2sh' | 'p2wsh') => {
    script: Buffer;
    address: string;
};
